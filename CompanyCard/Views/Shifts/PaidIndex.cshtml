@model IEnumerable<CompanyCard.Models.PaidShift>

@{
    ViewBag.Title = "PaidIndex";
}

<h2>PaidIndex</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Month_Year)
        </th>
        <th></th>
    </tr>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Ajax.ActionLink(item.Month_Year.ToString(), "PaidShiftInsideMonth", "Shifts", new { id = ViewBag.EmployeeId, month = item.Month_Year }, new AjaxOptions { LoadingElementId = "loadingImage", OnBegin = "onAjaxBegin()", OnSuccess = "onAjaxComplete()", UpdateTargetId = "mainBody", InsertionMode = InsertionMode.Replace, HttpMethod = "GET" })
                </td>
            </tr>
        }
    </tbody>
</table>
<table class=" bottom table table-striped">
    <tr>
        <td>Employee:</td>
        <td>@ViewBag.EmployeeName</td>
    </tr> 
    <tr>
        <td>How many Days worked:</td>
        <td>@Model.Count()</td>
    </tr>
    <tr>
        <td>Salary:</td>
        <td>@ViewBag.Salary</td>
    </tr>
</table>
<span>
    @Ajax.ActionLink("Back to List", "Index", "Employees", new { id = ViewBag.CompanyID }, new AjaxOptions { LoadingElementId = "loadingImage", OnBegin = "onAjaxBegin()", OnSuccess = "onAjaxComplete()", UpdateTargetId = "mainBody", InsertionMode = InsertionMode.Replace, HttpMethod = "GET" }, new { @class = "btn btn-primary btn-sm" })
</span>